import React from 'react';

const Table = ({data}) => {
    const {image,gamename,description,rating,year,genre,email, name} =data;


    return (
        <div>
         
        </div>
    );
};

export default Table;